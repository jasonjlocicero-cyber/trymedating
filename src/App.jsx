import React from 'react'

export default function App() {
  return (
    <div style={{padding:40, fontFamily:'ui-sans-serif, system-ui'}}>
      <h1>TryMeDating — minimal ✅</h1>
      <p>If you can see this after deploy, the toolchain is healthy. We’ll add router next.</p>
    </div>
  )
}
