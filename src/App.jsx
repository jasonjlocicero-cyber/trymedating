// src/App.jsx  — minimal smoke test
import React from 'react';

export default function App() {
  return (
    <div style={{ fontFamily: 'ui-sans-serif, system-ui', padding: 40 }}>
      <h1>TryMeDating — build smoke test ✅</h1>
      <p>If you see this after the Netlify deploy, the toolchain is fine. Next, we’ll restore the full UI.</p>
    </div>
  );
}
